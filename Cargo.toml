[workspace]
resolver = "2"
members = [
    "crates/codegraph-types",
    "crates/codegraph-db",
    "crates/codegraph-extraction",
    "crates/codegraph-resolution",
    "crates/codegraph-graph",
    "crates/codegraph-vectors",
    "crates/codegraph-context",
    "crates/codegraph-sync",
    "crates/codegraph-mcp",
    "crates/codegraph-core",
    "crates/codegraph-cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["CodeGraph Contributors"]
license = "MIT"
repository = "https://github.com/codegraph/codegraph"
rust-version = "1.75"

[workspace.dependencies]
# Core utilities
thiserror = "2.0"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Database
rusqlite = { version = "0.32", features = ["bundled", "blob", "functions", "vtab"] }

# Tree-sitter (core + grammars)
# Updated to latest versions (2026-02)
tree-sitter = "0.26"
tree-sitter-typescript = "0.23"
tree-sitter-javascript = "0.23"
tree-sitter-rust = "0.24"
tree-sitter-python = "0.25"
tree-sitter-go = "0.25"
tree-sitter-java = "0.23"
tree-sitter-php = "0.24"
tree-sitter-c = "0.24"
tree-sitter-cpp = "0.23"
tree-sitter-c-sharp = "0.23"
tree-sitter-ruby = "0.23"
tree-sitter-bash = "0.25"
tree-sitter-dart = { package = "tree-sitter-dart-orchard", version = "0.3" }
tree-sitter-swift = "0.7"
tree-sitter-graphql = "0.1"
tree-sitter-hcl = "1.1"
# Note: Kotlin blocked by tree-sitter version constraint (>=0.21, <0.23)

# File system
walkdir = "2.5"
globset = "0.4"
ignore = "0.4"
notify = "7.0"
sha2 = "0.10"

# Embeddings (optional feature)
# Note: ort features enabled per-crate based on feature flags
# Base ort without CoreML (for non-Apple Silicon platforms)
# std feature required for commit_from_file, ndarray for array integration
ort = { version = "2.0.0-rc.11", default-features = false, features = ["std", "ndarray", "download-binaries", "tls-native"] }
tokenizers = "0.20"
ndarray = "0.16"
tiktoken-rs = "0.6"

# CLI
clap = { version = "4.5", features = ["derive"] }
indicatif = "0.17"
console = "0.15"

# MCP Server
serde_derive = "1.0"

# Testing
tempfile = "3.14"

# Async (minimal, only for MCP)
# Note: We use sync by default, async only where needed

[workspace.lints.rust]
unsafe_code = "deny"
missing_docs = "warn"

[workspace.lints.clippy]
all = "warn"
pedantic = "warn"
nursery = "warn"

# Profile for development (fast compile)
[profile.dev]
opt-level = 0
debug = true

# Profile for release
[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = true
